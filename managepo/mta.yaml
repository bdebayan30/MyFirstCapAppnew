_schema-version: "3.2"
ID: mycapmanagepo
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: mycapmanagepo-destination-content
  type: com.sap.application.content
  requires:
  - name: mycapmanagepo-destination-service
    parameters:
      content-target: true
  - name: mycapmanagepo-repo-host
    parameters:
      service-key:
        name: mycapmanagepo-repo-host-key
  - name: mycapmanagepo-uaa
    parameters:
      service-key:
        name: mycapmanagepo-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: mycapmanagepo_html_repo_host
          ServiceInstanceName: mycapmanagepo-html5-service
          ServiceKeyName: mycapmanagepo-repo-host-key
          sap.cloud.service: mycapmanagepo
        - Authentication: OAuth2UserTokenExchange
          Name: mycapmanagepo_uaa
          ServiceInstanceName: mycapmanagepo-xsuaa-service
          ServiceKeyName: mycapmanagepo-uaa-key
          sap.cloud.service: mycapmanagepo
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: mycapmanagepo-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: mycapmanagepo-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - mycapmanagepo.zip
      name: mycapmanagepo
      target-path: resources/
- name: mycapmanagepo
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: mycapmanagepo-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: mycapmanagepo-destination-service
    service-plan: lite
- name: mycapmanagepo-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      tenant-mode: dedicated
      xsappname: mycapmanagepo-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-name: mycapmanagepo-xsuaa-service
    service-plan: application
- name: mycapmanagepo-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: mycapmanagepo-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
